#include "h.h"


main(int argc, char* argv[])
{

	if(argc > 1)
	{
		disk = argv[1];
	}

	fd = open(disk, O_RDWR);

	if(fd < 0)
	{
		printf("couldn't open %s\n", disk);
		exit(1);
	}

	init();
	mount_root();
}
